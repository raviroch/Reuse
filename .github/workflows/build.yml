name: Build and check a Docker image core

on: push

jobs:
  build1:
    uses: AshwiniRanganath2020/newflow-cd/.github/workflows/sample.yml@main
    with:
      TAG: python-check
      USER: raviroch
      # REGISTRY: ghcr.io
      VERSION: v1
